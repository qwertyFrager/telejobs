import { Input } from '@/shared/Input';

const InputModal = () => {
  return (
    <div>
      <Input className="h-8 w-full mt-2.5" placeholder="Position, Resume" />
    </div>
  );
};

export default InputModal;
